# phishing-detection
A phishing detection project that loads and cleans URL feature data, encodes categorical values, normalizes numeric fields, trains a RandomForest classifier, evaluates model performance, and saves the cleaned dataset and trained model for phishing URL classification.
